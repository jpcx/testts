{
  "name": "testts-demo",
  "version": "0.1.0",
  "description": "Demonstration for README image",
  "scripts": {
    "clean": "rm -f demo.pass.test.d.ts && rm -f demo.pass.test.js && rm -f demo.fail.test.d.ts && rm -f demo.fail.test.js",
    "build": "tsc --pretty -p .",
    "test-pass": "npx testts demo.pass.test.js",
    "test-fail": "npx testts demo.fail.test.js",
    "test": "npm run-script test-pass && npm run-script test-fail"
  },
  "author": "Justin Collier",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "@jpcx/testts": ">=0.1.0"
  }
}
